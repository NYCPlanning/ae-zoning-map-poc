import {
    NavigationControl,
    ScaleControl,
    AttributionControl,
    useMap,
    Map,
  } from "react-map-gl/maplibre";
import { TerraDraw, TerraDrawMapLibreGLAdapter, TerraDrawRectangleMode } from "terra-draw";
import { MapLibreGL as lib } from "maplibre-gl";
import { useRef } from "react";

export function getDraw(){
    // const map = useRef<maplibregl.Map>();
    console.log("ineditablelayer");
// console.log(map);
// const draw = new TerraDraw({ adapter: new TerraDrawMapLibreGLAdapter({ map, lib }), modes: [new TerraDrawRectangleMode()], });

}

